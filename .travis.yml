language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-koaYouth' ]; then cd .. && eval "mv $currentfolder generator-koaYouth" && cd generator-koaYouth; fi
